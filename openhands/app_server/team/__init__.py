"""AI Team — a supervised multi-agent engineering team on OpenHands.

See ``docs/design/ai-team.md`` (design) and ``docs/design/ai-team-impl-spec.md``
(implementation spec). Opt-in via the ``ENABLE_AI_TEAM`` env flag.

Phase 1 (this commit): the event/audit spine + data store + models + labels.
"""
